ktor {
    deployment {
        port = 80
    }

    database {
        url = "jdbc:sqlite:file:memory?mode=memory&cache=shared"
        driver = "org.sqlite.JDBC"
        username = "root"
        password = "root"
        database = "rentacar"
    }
}