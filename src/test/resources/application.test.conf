ktor {
    deployment {
        port = 80
    }

    database {
        url = "jdbc:h2:mem:regular;DB_CLOSE_DELAY=-1"
        driver = "org.h2.Driver"
        username = "root"
        password = "root"
    }

    upload_dir = "test_uploads"
}